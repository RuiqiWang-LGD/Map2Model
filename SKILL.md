---

name: map2model

description: Create editable CAD bases from site imagery, or clean existing CAD and generate editable SketchUp faces, using optional satellite imagery to help classify roads, water, greenery, and buildings. 适用于卫星图、航拍图或简化图转 CAD，以及已有 CAD 修线、SU 封面、分区分类和局部修正。

metadata:

  version: "0.9.8"

---

# Map2Model

让设计师从一张资料有限的场地图，尽快进入可编辑、可分面、可推拉的前期建模状态。承担描路、描河、整理地块与建筑、修线成面的重复工作。交付的是前期建模底图；不把单幅影像中的推定基底、路段或高差说成测绘成果。

已有 CAD 也可以直接上传：由 Codex 检查并整理线网，在 SketchUp 中封面，形成可编辑、可分区上色和推拉的模型；有配套卫星图或正射图时，还可结合 CAD 图层、文字和影像辅助识别道路、水体、绿地与建筑。 表达深度仍采用片区结构与场地详图两种模式；已有 CAD → SU 是使用入口。

默认成果执行 [SU 前期平面合同](references/su-plan-contract.md)：一套可见共面分区，连续同层道路、水面、主要父块和内部对象可按预期独立选面。主路/桥面占地裁掉内部低优先线；大水陆边界保留一条有作用的线，省略无独立用途的窄岸带，规整栅格锯齿。用户明确要求详细高差时按合同的详细分支处理。图层分类、几何 pass 或自生成面清单都不能代替实际选面对应。

## 片区结构模式：最高优先级取舍

**仅片区/块面模式适用：完整块面与一二级道路优先，三级道路及小碎细节默认省略，只有必要连接或主要分区例外保留；建筑先筛选再规整，不重要、歪扭或干扰边界的建筑可整项舍弃。桥河交汇从二维阶段清掉低优先穿线和无用窄带。** 生图、二维处理、CAD 和验收均先读 [片区最高优先级规则](references/district-priority.md)。在片区范围内，本规则高于本技能旧文及场景模块的细节保留要求；详图模式不变，混合模式的明确详图区不套用片区省略策略。原图定位、用户指定对象与几何质量要求继续成立。

## 对话与成果版本

面向设计师的回复使用“当前成果、需要决定的事项、下一步动作”的顺序。首次引导只列三项：建议处理模式、尺度参照、设计范围；末尾用自然的一段话说明怎么回复和接下来会做什么，不设第四项，不询问生图路径。结尾示例：“你把已知尺寸和范围告诉我就好，也可以发一张标注图。暂时都没有的话，回复“先整理全图”，我就开始生成简化平面，并对照原图检查、调整，再把转 CAD 推荐稿给你看。” 已有条件不重复索要，结尾按实际缺项调整。正常业务回复不追加“本轮先收集条件，依据你调用的技能”等规则解释、技能路径或条款引用；更高层级指令明确要求披露时按该要求执行。阶段交接只推荐一张已复核图，给出保存位置和下一步，其他修改稿留作备选。用户主动审查或修改技能时可以讨论规则。

用户提供设计范围后，按 [设计范围与成果版本](references/design-scope.md) 准备两张最终二维平面图：现状整理版，以及范围内仅留道路、其余清空为白色的设计范围清空版。设计范围不是仅提高精细度的重点区；范围外和未标注孔洞保留。无设计范围只做现状整理版，不猜范围、不清空全图。明确选定版本后只推进该版。

Codex 内置生图时，初稿、返修前图和中间候选只用于内部复核，不主动展示。复核完成后只主动提供一张推荐稿；有设计范围仍准备两个用途版本，另一版保存在图像参考目录，简短说明其用途，用户要求比较时再展示。推荐不等于用户已经选定。工具强制预览的能力限制见 [第一阶段](references/image-stage.md#执行内置生图)。

项目自动按“场景主题＋项目用途”命名，例如“水田农宅项目_建模底图”。用户指定路径时，在该路径下创建项目文件夹；未指定时，在系统实际桌面目录下直接创建这一层项目文件夹。用户明确指定项目名、完整项目目录或要求直接存放时优先沿用，已有项目续作不重复套层。默认仅交付 `成果`（按请求提供 DWG/SKP）与 `其他`（最终二维平面、原图叠合等）；推荐稿也放图像参考目录。开始生成前先按 [外部任务工作目录](references/output-lifecycle.md#外部任务工作目录) 确定交付目录与外部工作目录。提示词、旧稿、脚本、检查及转换材料从第一次写入起就在 Codex 工作区的独立任务目录生成，禁止在交付项目内创建 `process` 或临时文件；收尾只复制需交付的成果与图片；仅用户要求保留时将所需提示词或记录放入 `其他`。命名、重名处理与目录语义见 [项目命名与保存位置](references/output-lifecycle.md#项目命名与保存位置)。首次三项引导前可用一句话说明项目名称和拟保存位置，不新增命名确认项；未保存时不声称已创建或已保存。

## 开始执行

**已有 CAD 要求 SU 封面时，先走 [已有 CAD → SU 封面入口](references/existing-cad-to-su.md)。** DWG/DXF 是当前几何基线；读取图层、单位、实体与可见范围，整理线网并构造实际面。卫星图可辅助对应与分类，不是封面的必备输入。不套原图首次三项引导或生图流程；已有条件直接沿用，只澄清影响修复或范围的关键缺项。用户要求封面即授权生成 SKP，要求 CAD＋SU 即交付两者。

**先判断并推荐处理模式。** 查看原图整体和代表性局部，读取 [两种处理模式](references/processing-modes.md)。大范围且小对象模糊、只需环境框架时推荐片区结构模式；具体场地轮廓清楚且需要细化时推荐场地详图模式。综合用途、范围及可辨程度，不按像素阈值判断。建议模式放在首次引导第一项；已有模式沿用，用户要求直接开始时说明建议后执行，不增加单独确认轮次。模式从简图生成贯穿 CAD 和验收，不是临时结构审查阶段。 主体清楚而仅局部细碎时，第一项主动建议“主体详图＋局部片区概括”，点明位置、可靠程度和首轮取舍，供用户同轮确认或调整；不因边角难点将全图降级，具体话术与执行见两种模式页。

**所有类型的低清晰度输入先保护结构。** 无论城市、乡村、园林或其他场地，只要内部形体难辨，读取 [低清晰度重构与道路退让](references/low-clarity-reconstruction.md) 和 EF012-C：内部先核验道路与块面并固定基线，再安排本模式筛选保留的道路与对象；片区结构模式中的难辨小建筑按片区边界概括，不重构单体，默认完成全部检查后一次交付，不因模糊而强制用户先审结构。重构的建筑、花池等须留足与模型尺度相适应的道路间距；放不下就简化、合并或省略细节，不缩路、不移块。局部模糊只处理局部；清晰真实共边与道路入口按其实际关系处理。间距、重叠面及 SU 编辑隔离分别验证。

**复杂城市片区增加内部检查，默认一次交付。** 多街坊、主次路交织或密集小建筑时，读取 [结构优先与按需分步](references/complex-urban-two-stage.md) 和 [EF012 当前规则](references/error-library/cases/EF012.md)。内部按本模式筛选后的道路与块面先行、再安排保留建筑及必要细节构造，结构核验后固定基线，新增细节后复检完整路网；复杂程度只决定检查强度，不自动增加用户审核轮次。仅当用户明确要求分步，或道路类别、连接、入口等关键结构经自查仍无法可靠判断且会改变块面关系时，才交结构成果或局部证据请用户决定，等待期间可继续独立部分。单纯细节模糊、工作量大或进入新模块不构成暂停理由。用户明确取消旧的强制阶段审查时记录其已被覆盖，复用有效基线继续；真实未决结构问题不因此消失，不能伪造结构通过。既有图像选版、直接 CAD 授权与设计范围规则保持。

1. 查看上传图片，分清原始影像、简化分析图、风格参考及已验收 CAD。**首次引导用一条独立的普通聊天回复，固定列出三项：1．建议处理模式（附一句图像依据与主要取舍）；2．尺度参照；3．设计范围。已有答案写明已知条件，不重复提问。末尾采用“对话与成果版本”中的自然收尾，邀请用户回复文字或上传标注图；不增加第四项或路径选择，不调用弹出式问答工具。** 用户已提供完整条件、说明未知或要求直接开始时，沿用条件并执行，不为模板增加停顿。缺项只集中收集参照物尺寸（对象、位置/两端、数值、单位、测量含义）和设计范围；不能把倾斜屋顶长边当地面标尺。范围可文字说明或在原图副本上涂色标注，保留未标注原图。用户不知道尺寸或暂不指定范围时，按相对尺度及已选/推荐模式整理全图现状，不自行指定重点区。输入约定见 [项目设置](references/project-contract.md)。

2. **先读 [共用判断规则](references/shared-rules.md)**。同一规则从生图阶段就生效：先确认尺度与路网关系，构造顺序为一级道路 → 围合块面 → 二三级道路 → 建筑及其他细节。原图提供可见地面位置依据，分析图帮助理解；发现两者冲突时对相应区域查证。

3. 尚待简化的原图任务，按 [第一阶段的使用引导](references/image-stage.md) 整理项目条件，选择适用场景模块，运行 `scripts/build_prompt.py` 合成完整提示词，由 Codex 使用当前可用的内置生图工具执行。取消主动提供“输出提示词→其他平台生成→回传”的路径，也不将其作为工具不可用时的默认退路。提示词在执行期间内部保存并用于生成、修改和追溯，默认不永久交付；对照原图复核并按需局部修正，筛选一张转 CAD 推荐稿。已有简化图、明确直接转 CAD 或局部修改时走对应入口，不强制重新生图。

4. **图像交接按来源区分。** Codex 内置生成并修改后的图，先将一张推荐稿放入项目的 `其他`，简短说明版本与位置，再确认是否据此转 CAD；用户同意明确推荐稿、指定其他稿或拖入某稿要求转换即授权继续，不要求重复上传本地可读文件。有设计范围仍准备两个用途版本，按用途推荐其中一张，另一张作为备选。外部平台回传/直接导入的图不加这道通用确认；有具体改善建议时说明问题、影响和拟改内容，让用户选择是否采用，用户否定建议就按回传图转 CAD。用户已明确指定图像并要求转换时直接沿用授权，不重复询问。具体话术与分支见 [图像交接](references/image-stage.md#交接到-cad)。

5. 新场地从零建立证据和对象清单；已有成果只修改授权范围，保存基线并检查相邻接边。不能因为新分析图更整洁就全图重建。

| 当前输入/请求 | 读取与行动 |

|---|---|

| 原始图，需要先简化 | [第一阶段](references/image-stage.md)：三项条件引导 → 内部项目提示词 → Codex 内置生成 → 对照原图复核与局部修改 → 提供一张转 CAD 推荐稿 |

| 已有简化图，开始 CAD | [CAD 执行](references/cad-stage.md)；清楚围合的路网/色块采用 [CAD 分面模块](references/cad-modules.md)，先外层后内部 |

| 直接从原图做 CAD | 按 CAD 执行分对象处理，可跳过生图；仍适用共用规则 |

| 已有 CAD，需要 SU 封面/分类 | [已有 CAD → SU](references/existing-cad-to-su.md)；检查并修线、建立实际面，有影像时辅助分类，保存并回读 SKP |
| 局部修改已有 CAD | CAD 执行中的更新路径；记录基线、允许改动对象、实际修复与回归结果 |

## 分级围合与规则形体

先按模式确定保留对象，再按 **骨干道路 → 围合块面 → 本模式保留内路 → 保留建筑及细节** 构造与回归；片区骨架以一二级路为主，详图内路范围保持原要求。道路优先不等于内外道路全部合面；先按用途和用户目标确定同面/异面关系，再处理入口共边。圆角方块、规则条带等有依据的形体先用规则几何构造，避免航拍畸变驱动碎折线。执行 [分级围合与规则几何](references/hierarchy-and-regular-geometry.md)，区分独立选面、独立编辑和封闭体块；未定位的 SU 联动不能用几何 pass 或“没有编组”直接解释。

## 原图边界优先：现状定位的阻断检查

明显河道、洲岛、主路等必须通过 [明显地物定位与相对宽度检查](references/source-reference-lock.md)：先建原图独立控制点与断面，分别核对双侧位置、道路宽度及道路/参照物宽度比；没有真实尺寸也不允许任意加粗。上下游或主要弯段失败时不能标为定位合格，先按原图修正，再推荐或推进 CAD。

原图可见地面边界高于生成二维图的位置与宽度。用户选定简图不等于认可其位移；道路与父块的原图叠图复核未通过，不得推进依赖细节或凭闭合检查交付。先区分车行道路缘、人行道外缘与绿地内缘。发生重复边界反馈时先重开证据基准，执行 [原图边界关卡](references/shared-rules.md#原图边界关卡) 并检索 [EF010-B](references/error-library/cases/EF010.md#ef010-b)。

## 模式与细化范围

先继承已选模式、项目用途与授权范围；完整取舍及停止条件见 [两种处理模式](references/processing-modes.md)。片区结构模式优先保留一二级道路、水系、完整大片区及筛选后的重要规整建筑；三级路仅保留必要例外，小碎、不重要或歪扭建筑省略并归入背景片区。片区与建筑分层，不进入建筑逐栋验收；实际保留建筑和全部可见线网仍按合同检查。

场地详图模式及用户授权局部详图区沿用已有建筑二维基底门、绿地和内部步道逐对象检查，读取 [细节检查](references/site-detail-checks.md)。旧文中的“大尺度方法”对应片区表达，“中小尺度场地详图”对应详图表达；具体适用范围以已选模式及可辨证据为准，不因图片像素或建筑较少擅自升级全图。局部模糊只限制局部，不凭空补细节。

全图片区模式可附明确的局部详图区；设计范围仍按用途制作清空版，不能自动等同精绘范围。片区模式达到保留对象、结构、分面及回读检查要求就交付，不为被主动省略的模糊细节反复全图生成。尚未确认的主要结构问题仍按证据处理。

## 必须落实的判断

- **道路先于建筑。** 已识别道路的缺失段按邻近可见宽度、走向及合理接入点进行最少改动的连续推定；不能因投影建筑或遮挡而留断。真实尽端、隔水关系和不同通行层级分别处理。推定不等于精确还原。

- **倾斜建筑重建基底。** 不能仅擦去立面线后沿用被高度拉长的外包络。基底放在相应道路与场地范围内；局部调整、同街坊概括，避免所有建筑统一缩小或跨路挪动。中小尺度且建筑数量可逐栋处理时，按 [建筑平面化](references/building-regularization.md#中小尺度建筑的二维基底门) 逐栋复核、修正后再推荐；明显伪二维不能仅标注“留待 CAD”就作为推荐稿交接。

- **主要场地外围先清楚。** 保留实际出入口、孔洞和有独立用途的岸上对象；默认大水陆界只留一条有意义的边，详细高差按明确请求保留。圆弧保持曲率特征。水面泡沫、跌水亮带默认不成线、不成路，真实交通桥保留。

- **外层通过后再加内部。** 一级道路与父块先独立成立，再安排归属明确且本模式保留的内路，最后添加筛选后的建筑、绿地和内院。直边保直、曲线控制分段；检查原始 CAD 的面与对象对应，新增细节后全图重查路网，不能只比较旧图层。模块与批量检查见 [CAD 分面模块](references/cad-modules.md)。

- **异常建筑先分清屋面与地面。** 夸张碎角、细长尾巴或铺装串联时，按 [建筑细化](references/building-regularization.md) 批量筛选、对照源图并局部规整。正常楼体与真实曲线/组合形体保留；不能只凭颜色、顶点数或矩形度自动改楼。

- **简化不等于全局重排。** 颜色分割、轮廓提取、平滑只是候选生成方法。不能用整图变形让分析图看起来贴合原图，也不能把坐标/实体编号补丁包装成通用识别。

- **不确定小块按关系处理。** 优先保持一二级道路连续、干净、有宽度，再依据路网、邻接和用途合理封面、归并中小块；不机械补缝或全保像素碎面。重点筛查大范围道路边和建筑边的明显锯齿，合理小转角与短边保留。每次测试按 [案例复核与复用](references/case-review.md) 记录“问题—源图依据—方案—验证结果”，供后续检索与错误预判；这不是模型自动训练，也不证明陌生图片已解决。

**碎图封面优先级：一级道路清晰且有可用宽度 → 围合块面独立可选 → 本模式保留道路接续 → 筛选后的建筑及细节 → 像素贴合。** 当碎线、细缝、残三角妨碍分面时，可在本轮授权区域牺牲局部轮廓精度，按用途拉直、简化或并入相邻地块；真实通道、入口和必要孔洞不能吞并。收到实际封面失败反馈时，将对应区域重置为未验收，执行 [局部封面检查](references/region-face-check.md)，不能沿用全图脚本 pass。检索 [独立错例库](references/error-library/index.md)，把旧漏检转为可重复执行的失败场景。

## 统一错例库：检索后执行，反馈后维护

开工先读 [错例索引](references/error-library/index.md)，只加载相关问题族和必要图片，把对应检查加入本图。异常建筑、主路/桥面、碎地块和实际分面是不同验收项，不互相替代。建筑小也不能放过夸张碎角、拖尾和无结构多边形；按 [建筑细化](references/building-regularization.md) 覆盖全部应保留建筑，逐项处置，不只修用户圈出的样本就称全图检查完成。

测试中出现新反馈、检查失败或修复结果，执行 [错例维护流程](references/error-library/maintenance.md)：本任务内保存旧新证据，按原因/修法/检查/适用结构归类；完全重复不再存图，有新信息则保留变体。已授权维护且可写时同步本地 Skill；只读安装在执行期间暂存项目增量，收尾按文件保留与清理规则处理，不声称后台自动更新或向其他用户/GitHub上传。本库用于避免已知遗漏，不保证零错误。

## 验收与交付

对象关系已判明后，优先使用 [CAD 自动执行](references/cad-automation.md)：项目配方驱动外层/建筑构造和有限相邻修边，自动生成复核页，并串联检查、转换与续跑。它复用本 Skill 的分面合同；新场景替换数据和容差，不能替换源图判断。

读取 [检查与纠错](references/acceptance.md)，按“对象—证据—失败条件—修复—复检”记录。先建独立源侧对象/探针和同面关系，再检验实际完整导出线网；主路内部干扰、边界多线、形体锯齿与选面碎裂均有单独门。视觉判断与脚本检查分别留证；未检查写 pending，不能自动填 pass。修复优先影响道路、主要外围、尺度与基底的问题；每次局部修复后复检相邻连接和已验收部分。

几何检查使用 `python scripts/check_cad.py --help`；独立多边形父/子环增加 `scripts/check_face_hierarchy.py`。默认可见平面同时运行 `scripts/check_visible_plan.py`，用独立 `source-contract.json` 检查完整线网、源侧同面关系、主路内部、单边界、宽度和锯齿；命令见 [SU 合同](references/su-plan-contract.md)。输出要求、候选矢量化和叠图命令见 CAD 执行。脚本通过不能替代道路连通判读、基底判断或原图视觉复核；不必逐条肉眼检查碎线。原图独立检查点不得取自待检 CAD 自身。

影像转 CAD 默认交付 **一个真实 DWG＋一张卫星/正射原图叠图 JPG**。已有 CAD → SU 封面入口以真实 SKP 为主要成果；用户要求 CAD＋SU 时交付 DWG 与 SKP，具体见该入口，不受默认两项清单限制。取消二维简图叠图，不再生成或交付该类叠图；执行中检查记录、配准、日志及内部复核页留过程目录，不混入最终交付文件夹；最终验收并核对保存完成后，按 [文件保留与清理](references/output-lifecycle.md) 精简为成果与图片两类：保留当前 DWG/SKP（按任务范围）、最终二维图、其他有效用途版及原图叠合；提示词、修改指令、旧候选、脚本、日志与检查材料默认清理，不另建永久过程存档或修改记录。用户要求的提示词、历史稿或可重跑归档例外保留。已明确授权的同一清理范围无需重复确认，授权与审批恢复按该清理页执行。原图叠图必须使用最终 DWG 的回读几何，执行与验证期间记录源图、变换与哈希，收尾按文件保留与清理规则处理。缺原始影像时先完成 CAD，并说明原图叠图待补，不用简图或生成图替代。具体命令见 [最终交付](references/cad-stage.md#叠图与最终交付)。生图阶段用于选版的最终二维平面图继续保留，与已取消的 CAD 简图叠图不同。

DWG 须由实际转换器输出并回读。没有可用转换器时保存可编辑 DXF 并说明尚缺 DWG；禁止改后缀冒充完成。SU 测试、全局安装、发布和收费图像接口按用户实际请求执行。

首版验证范围与局限见 [验证记录](evals/validation.md)。本 Skill 不依赖 Superpowers 或本机专用路径运行。



细节优化时优先拦截隔离带无用途内圈、花池拼接尖钩和铺装树冠误生绿带，执行 [细节的同类反馈检查](references/site-detail-checks.md#同类反馈的优先检查)。用户认可本场景只更新对应版本；新图仍从原图建立独立期望。
